[project]
name = "instagram-dm-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
# Direct dependencies only — transitive versions are resolved by uv.lock, and
# requirements.txt carries the fully pinned set for plain pip installs.
dependencies = [
    "fastmcp==3.4.7",
    "instagrapi==2.18.14",
    "pillow>=8.1.1",
    "python-dotenv==1.2.2",
]
